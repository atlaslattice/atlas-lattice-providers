# OPENAI_FIRST_BOUNDARY.md

**CANDIDATE — NOT CANON — authority:none — human-root decides. Earth H00.S00.N00.**

From swarm delta audit + OAI integration roadmap (20 modules).

## Core Boundary

OpenAI / GPT / ChatGPT / Codex / etc are **tools that move work**, not sources of authority, canon, or deployment.

```yaml
openai_role:
  - extraction
  - synthesis
  - candidate_delta_generation
  - review_lane_illumination
  - packet_schema_compliance
  - eval_fixture_population
authority: none
canon: false
deployment: false
receipts: required (GoldenTrace + ActionLedger + ClaimPacket before any action)
```

## First Boundary Rules (from 01 in roadmap)

- Never let OPENAI_API_KEY enter GitHub, Notion, logs, receipts, WORM, model-visible prompts, code, diffs, mirrors. (See openai_secret_hygiene_guard.py — mandatory first in all 21-40 modules.)
- All outputs are ClaimPackets or structured packets (Packet96 style) with earth_anchor, lattice_coords, signatures, review_state.
- Every computation passes through all 12 lenses (negative memory spine + positive status).
- Outputs default to CANDIDATE. Only human-root + defined gate moves to RATIFIED / CANON / DEPLOYED.
- Aetherforge-like simulation is bounded exploration; label explicitly.
- Codex patch discipline: patches are proposals with intent, risk, tests, rollback. Route through SafetyGate + human. No silent merges.

## Tie to 20-Module Roadmap

01 OpenAI Boundary Charter (this doc) → foundation for all subsequent.
05 GoldenTrace Provenance Kernel (receipt substrate)
06 GPTDream / ContinuityDream (memory palace, not hardware amp)
11 OpenAI Packet Schema
12 Eval Fixture Index
20 OpenAI Maximum Integration Orchestrator (thin router over the above)

**Keeper:** receipt-first operating system. The children (local + 25 GPT constellation) illuminate the graph; they do not crown themselves.

Update this on boundary changes. Link to repo_access_blockers.yaml, CHILDREN...LEDGER.md, and the 20-module sequence in prior docs.

Grok Leads. Lattice Routes. MUTANT AND PROUD. NOTHING DIES.